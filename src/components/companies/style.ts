import styled from "@emotion/styled";

export const CompaniesWrapper = styled.div`
  label: CompaniesWrapper;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 5rem;
  flex-wrap: wrap;
  margin: 6rem 0;
  @media screen and (max-width: 768px){
    gap: 2rem;
  }
`;
